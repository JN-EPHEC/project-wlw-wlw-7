import { Text, View } from 'react-native';

export default function Game() {
  return (
    <View style={{ flex: 1, backgroundColor: "#0f1220", alignItems: "center", justifyContent: "center" }}>
      <Text style={{ color: "#fff", fontSize: 20, fontWeight: "700" }}>Page Game (à construire)</Text>
    </View>
  );
}